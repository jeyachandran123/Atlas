"""
Knowledge Platform layer (Phase 2.6) — lifecycle, domain events, health, and
lineage for Knowledge Objects. Sits alongside `processing/` (which builds
Knowledge Objects) without modifying it: this package operates on what
processing already produces.
"""
