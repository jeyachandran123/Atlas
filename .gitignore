# Python
__pycache__/
*.py[cod]
*.pyo
venv/
.env
*.egg-info/
dist/
build/

# Database
*.db
*.sqlite3

# Logs
*.log
logs/

# ChromaDB data
chroma_data/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Firebase credentials (NEVER commit)
firebase-service-account.json
*-service-account.json

# Real CCTV source footage — kept on disk, never in git.
# Identifiable workers appear in every frame, and git history is permanent.
# These files are the only PPE-violation footage in the project: archive them
# somewhere durable, because ignoring them means this machine is their only copy.
media/
